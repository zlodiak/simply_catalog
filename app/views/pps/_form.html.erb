<%= form_for(@pp) do |f| %>
  <% if @pp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pp.errors.count, "error") %> prohibited this pp from being saved:</h2>

      <ul>
      <% @pp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
